#version 450
layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) buffer ParticleIdx {
    ivec2 particleIdx[];
    // idx 0 : cellIdx
    // idx 1 : slot
};

layout(std430, binding = 1) buffer IdCells {
    uint idCells[];
};

layout(std430, binding = 2) buffer Starts {
    uint starts[];
};

layout(push_constant) uniform ParamsPos {
    uint numParticles;
} pc;

void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i >= pc.numParticles) {
        return;
    }

    ivec2 p = particleIdx[i];
    int cell = p.x;
    int slot = p.y;

    uint idx = starts[cell] + slot;

    idCells[idx] = i;
}